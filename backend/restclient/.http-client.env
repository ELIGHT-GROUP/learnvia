# REST Client environment variables for testing
# Copy values after running a successful OAuth flow

HOST=http://localhost:8080
FRONTEND_REDIRECT=http://localhost:3000/oauth/callback

# After auth flow set this to the JWT produced by the server redirect fragment
ACCESS_TOKEN=

# Example user id
USER_ID=
